# <%= @asset_data.name %>
<% @asset_hash['mutable']['maps'].each do |map| %>

## <%= map.first %>

### Port Layout

  <% map[1]['map'].each do |port, asset| %>
<%= port %>: <%= asset %>
  <% end %>
<% end %>
